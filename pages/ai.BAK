<!doctype html>
<html lang="ja">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>AI入門サイト｜1ページで分かる</title>
  <meta name="description" content="AIの基本、活用、導入、リスクまでを1ページで分かりやすく説明" />
  <style>
    :root {
      --bg: #0f172a;         /* slate-900 */
      --panel: #111827;      /* gray-900 */
      --panel-2: #0b1220;    /* darker */
      --text: #e5e7eb;       /* gray-200 */
      --muted: #94a3b8;      /* slate-400 */
      --brand: #60a5fa;      /* blue-400 */
      --brand-2: #38bdf8;    /* sky-400 */
      --ok: #34d399;         /* green-400 */
      --warn: #fbbf24;       /* amber-400 */
      --danger: #f87171;     /* red-400 */
      --radius: 16px;
      --shadow: 0 10px 30px rgba(0,0,0,.35);
      --maxw: 1080px;
    }
    * { box-sizing: border-box; }
    html { scroll-behavior: smooth; }
    body {
      margin: 0;
      font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, "Hiragino Sans", "Noto Sans JP", "Yu Gothic", "Meiryo", sans-serif;
      line-height: 1.7;
      color: var(--text);
      background: radial-gradient(1200px 800px at 20% -10%, rgba(56,189,248,.15), transparent),
                  radial-gradient(1000px 700px at 120% 10%, rgba(96,165,250,.10), transparent),
                  var(--bg);
    }
    a { color: var(--brand-2); text-decoration: none; }
    a:hover { text-decoration: underline; }

    /* Header / Hero */
    header.hero {
      position: relative;
      padding: 72px 16px 48px;
      border-bottom: 1px solid rgba(148,163,184,.2);
      background: linear-gradient(180deg, rgba(17,24,39,.6), rgba(17,24,39,.2));
    }
    .wrap { max-width: var(--maxw); margin: 0 auto; }
    .topbar {
      display: flex; align-items: center; justify-content: space-between;
      gap: 16px; margin-bottom: 24px;
    }
    .brand { display: flex; align-items: center; gap: 12px; font-weight: 700; }
    .brand .dot {
      width: 12px; height: 12px; border-radius: 50%;
      background: linear-gradient(135deg, var(--brand), var(--brand-2));
      box-shadow: 0 0 20px rgba(56,189,248,.6);
    }

    nav.quicknav { display: none; gap: 12px; flex-wrap: wrap; }
    nav.quicknav a { padding: 6px 10px; background: #0b1220; border-radius: 999px; border: 1px solid rgba(148,163,184,.2); }
    @media (min-width: 900px) { nav.quicknav { display: flex; } }

    .hero h1 { font-size: clamp(28px, 4vw, 44px); margin: 8px 0 12px; letter-spacing: .02em; }
    .hero p.lead { color: var(--muted); margin: 0 0 20px; font-size: clamp(14px, 2.2vw, 18px); }
    .cta { display: inline-block; padding: 12px 18px; border-radius: 12px; font-weight: 700; letter-spacing: .02em;
           background: linear-gradient(135deg, var(--brand), var(--brand-2)); color: #071018; box-shadow: var(--shadow); }
    .cta:hover { opacity: .95; text-decoration: none; }

    /* Sections */
    section { padding: 48px 16px; }
    .card { background: linear-gradient(180deg, var(--panel), var(--panel-2));
            border: 1px solid rgba(148,163,184,.18); border-radius: var(--radius); box-shadow: var(--shadow);
            padding: 28px; }
    h2 { font-size: clamp(22px, 3vw, 28px); margin: 0 0 12px; }
    h3 { font-size: 18px; margin: 22px 0 8px; }
    ul { margin: 8px 0 16px 1.2em; }
    li { margin: 6px 0; }

    .grid { display: grid; gap: 18px; }
    @media (min-width: 900px) { .grid.cols-2 { grid-template-columns: 1fr 1fr; } }
    @media (min-width: 900px) { .grid.cols-3 { grid-template-columns: 1fr 1fr 1fr; } }

    .badge { display: inline-block; padding: 4px 8px; border-radius: 999px; font-size: 12px; color: #0b1220; font-weight: 700; }
    .b-ok { background: var(--ok); }
    .b-warn { background: var(--warn); }
    .b-danger { background: var(--danger); }

    .table { width: 100%; border-collapse: collapse; margin: 12px 0 6px; }
    .table th, .table td { border: 1px solid rgba(148,163,184,.25); padding: 10px 12px; text-align: left; }
    .table th { background: #0b1220; }

    details { background: #0b1220; border: 1px solid rgba(148,163,184,.25); border-radius: 12px; padding: 12px 14px; margin: 10px 0; }
    summary { cursor: pointer; font-weight: 700; }

    /* Footer */
    footer { padding: 36px 16px; border-top: 1px solid rgba(148,163,184,.2); color: var(--muted); }

    /* Back to top */
    #backToTop {
      position: fixed; right: 16px; bottom: 16px; padding: 10px 12px; border-radius: 10px;
      background: #0b1220; border: 1px solid rgba(148,163,184,.25); color: var(--text);
      box-shadow: var(--shadow); cursor: pointer; display: none;
    }
    #backToTop:hover { opacity: .9; }
  </style>
</head>
<body>
  <header class="hero">
    <div class="wrap">
      <div class="topbar">
        <div class="brand"><span class="dot" aria-hidden="true"></span>AI入門サイト</div>
        <nav class="quicknav" aria-label="クイックリンク">
          <a href="#understand">AIとは</a>
          <a href="#cases">活用事例</a>
          <a href="#adopt">導入手順</a>
          <a href="#risk">リスク</a>
          <a href="#summary">要点</a>
        </nav>
      </div>
      <h1>AIを<strong>かんたん</strong>に理解して、<br/>今日から使い始めよう</h1>
      <p class="lead">AIの基本、活用、導入、リスクまで。むずかしい言葉なしで1ページにまとめたガイド。</p>
      <a class="cta" href="#understand">学びを始める</a>
    </div>
  </header>

  <main class="wrap">
    <!-- AIとは何かを今すぐ理解する -->
    <section id="understand" aria-labelledby="understand-title">
      <div class="card">
        <h2 id="understand-title">AIとは何かを今すぐ理解する</h2>

        <h3>基本概念</h3>
        <p>AIは「人の知る・考える・判断する」をコンピュータで再現する技術。今は、データから自分で学ぶ<span class="badge b-ok">機械学習</span>、より高度な<span class="badge b-ok">深層学習</span>が主流。文字・音声・画像・動画の理解まで広がっている。</p>

        <h3>活用領域</h3>
        <ul>
          <li><strong>ビジネス</strong>：売上予測、顧客分析、広告最適化</li>
          <li><strong>医療</strong>：画像診断、創薬の支援</li>
          <li><strong>製造</strong>：不良検出、設備の異常予測</li>
          <li><strong>教育</strong>：AI家庭教師、学習プラン作成</li>
          <li><strong>金融</strong>：不正検知、リスク評価</li>
          <li><strong>エンタメ</strong>：画像・音声・文章の生成</li>
        </ul>

        <h3>導入手順</h3>
        <ol>
          <li><strong>課題を決める</strong>（例：問い合わせ対応時間を半分に）</li>
          <li><strong>データをそろえる</strong>（まずは社内の記録を整理）</li>
          <li><strong>作るか使うかを決める</strong>（自作 or 既存ツール）</li>
          <li><strong>検証→改善</strong>（小さく試して良ければ広げる）</li>
        </ol>

        <h3>費用感</h3>
        <table class="table" aria-label="費用感テーブル">
          <thead>
            <tr><th>スコープ</th><th>目安コスト</th><th>コメント</th></tr>
          </thead>
          <tbody>
            <tr><td>小さく試す（ツール活用）</td><td>月1万〜10万円</td><td>API/ノーコードで素早く</td></tr>
            <tr><td>自社開発（小〜中規模）</td><td>数百万円〜</td><td>要件とデータの整備がカギ</td></tr>
            <tr><td>全社導入（大規模）</td><td>数千万円〜</td><td>基盤構築・運用体制が必須</td></tr>
          </tbody>
        </table>

        <h3>よくある疑問</h3>
        <details>
          <summary>数学が苦手でも使える？</summary>
          <p>使うだけなら大丈夫。多くの処理はツールが自動で実行してくれる。</p>
        </details>
        <details>
          <summary>中小でも意味ある？</summary>
          <p>ある。問い合わせ対応や事務の自動化から始めるのが効果的。</p>
        </details>
        <details>
          <summary>精度が出ない時は？</summary>
          <p>データの見直し、学習量アップ、アルゴリズム変更で改善できる。</p>
        </details>
      </div>
    </section>

    <!-- AIの活用法を実例で理解する -->
    <section id="cases" aria-labelledby="cases-title">
      <div class="card">
        <h2 id="cases-title">AIの活用法を実例で理解する</h2>
        <div class="grid cols-3">
          <div>
            <h3>マーケティング事例</h3>
            <ul>
              <li>SNS投稿の時間と内容を最適化</li>
              <li>広告文を自動生成して効果比較</li>
              <li>おすすめ商品を一人ひとりに出し分け</li>
            </ul>
          </div>
          <div>
            <h3>業務自動化事例</h3>
            <ul>
              <li>問い合わせにAIが一次返答</li>
              <li>請求書や経費の自動分類</li>
              <li>会議の文字起こしと要約</li>
            </ul>
          </div>
          <div>
            <h3>製品開発事例</h3>
            <ul>
              <li>レビューから改善点を自動抽出</li>
              <li>設計段階でのシミュレーション</li>
              <li>生成AIで試作デザインを作成</li>
            </ul>
          </div>
        </div>
      </div>
    </section>

    <!-- AIの導入を始める手順 -->
    <section id="adopt" aria-labelledby="adopt-title">
      <div class="card">
        <h2 id="adopt-title">AIの導入を始める手順</h2>
        <div class="grid cols-2">
          <div>
            <h3>要件定義</h3>
            <p>目的とKPIを明確に。例：「対応時間を50%削減」「売上を10%増」など。現場の声を聞き、効果が数字で分かる形にする。</p>
          </div>
          <div>
            <h3>ツール選定</h3>
            <ul>
              <li><strong>ノーコード型</strong>：ChatGPT、Azure OpenAI、Google Vertex AI</li>
              <li><strong>開発型</strong>：Python、scikit-learn、PyTorch、SageMaker</li>
            </ul>
            <p>社内スキルとデータの扱い方で選ぶのがコツ。</p>
          </div>
        </div>
        <div class="grid cols-2">
          <div>
            <h3>運用体制</h3>
            <ul>
              <li>定期の精度チェック</li>
              <li>データ更新の流れを作る</li>
              <li>現場からのフィードバック反映</li>
            </ul>
          </div>
          <div>
            <h3>小さく始めて広げる</h3>
            <p>まずは一部の業務でPoC（小さな実験）。良ければ周辺へ拡大。失敗しても学びが残る形にする。</p>
          </div>
        </div>
      </div>
    </section>

    <!-- AIのリスクを把握する -->
    <section id="risk" aria-labelledby="risk-title">
      <div class="card">
        <h2 id="risk-title">AIのリスクを把握する</h2>
        <div class="grid cols-3">
          <div>
            <h3>セキュリティ <span class="badge b-warn">重要</span></h3>
            <ul>
              <li>個人情報や社外秘の取り扱い</li>
              <li>アクセス制限とログの記録</li>
              <li>匿名化・マスキングの実施</li>
            </ul>
          </div>
          <div>
            <h3>倫理問題</h3>
            <ul>
              <li>偏った判断（バイアス）の回避</li>
              <li>人による最終チェック</li>
              <li>説明できる結果の提示（説明責任）</li>
            </ul>
          </div>
          <div>
            <h3>法規制</h3>
            <ul>
              <li>個人情報保護法への対応</li>
              <li>各国のAIガイドラインの確認</li>
              <li>導入前に専門家と相談</li>
            </ul>
          </div>
        </div>
      </div>
    </section>

    <!-- 要点まとめ -->
    <section id="summary" aria-labelledby="summary-title">
      <div class="card">
        <h2 id="summary-title">重要ポイントを短く整理する</h2>
        <ul>
          <li>AIは「人の判断をまねる技術」。</li>
          <li>成功のカギは「目的の明確化」と「データの質」。</li>
          <li>小さく試して、良ければ広げる。</li>
          <li>セキュリティ・倫理・法の対策をセットで。</li>
        </ul>
      </div>
    </section>
  </main>

  <footer class="wrap">
    <div>© 2025 AI入門サイト｜作成: あなた（AI助サポート）</div>
  </footer>

  <button id="backToTop" type="button" aria-label="ページ上部へ">▲ 上へ</button>

  <script>
    // 上へ戻るボタン（三項演算子は使わない）
    (function () {
      var btn = document.getElementById('backToTop');
      function onScroll() {
        if (window.scrollY > 400) {
          btn.style.display = 'block';
        } else {
          btn.style.display = 'none';
        }
      }
      function scrollTopSmooth() {
        window.scrollTo({ top: 0, behavior: 'smooth' });
      }
      window.addEventListener('scroll', onScroll);
      btn.addEventListener('click', scrollTopSmooth);
      onScroll();
    })();
  </script>
  <!-- 共通チャットボット埋め込み -->
  <iframe src="../chatbot.html" style="position: fixed; bottom: 20px; right: 20px; width: 400px; height: 600px; border: none; border-radius: 10px; z-index: 9999;"></iframe>
</body>
</html>
